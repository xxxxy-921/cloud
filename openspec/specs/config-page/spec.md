# Capability: config-page

## Purpose
~~Defines the system configuration management page, including listing configs in a table and CRUD operations via Sheet drawers and AlertDialog confirmation.~~

**DEPRECATED**: This capability has been removed. The raw KV config editor has been replaced by typed settings APIs and the settings page tabs. System configuration is now managed through `/settings` with dedicated tabs for each config category (security, scheduler).
